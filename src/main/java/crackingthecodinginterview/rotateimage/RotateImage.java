package crackingthecodinginterview.rotateimage;

/**
 * Given an image represented by an NxN matrix, where each pixel in the image is 4 byte,
 * write a method to rotate the image by 90 degrees. can you do this in place?
 */

public interface RotateImage {

    void rotate90(int[][] matrix, int size);
}